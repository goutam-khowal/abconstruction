import GalleryClient from "./GalleryClient";

export const metadata = {
  title: "Gallery – A&B Construction",
  description:
    "Exquisite surface installations, marble work, and architectural tile executions across India.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}